#include <stdio.h>
int main()
{
    int days, months, rem_days;
    printf("Enter total days: ");
    scanf("%d", &days);
    months = days / 30;
    rem_days = days % 30;
    printf("Months = %d\n", months);
    printf("Remaining Days = %d", rem_days);
    return 0;
}
