#include <stdio.h>
int main() 
{
    int age;
    printf("Enter your age: ");
    scanf("%d", &age);
    if (age >= 18)
{
        if (age >= 60)
{
            printf("Eligible for voting (Senior Citizen)");
        } 
else 
{
            printf("Eligible for voting");
        }
    } 
else
{
        printf("Not eligible for voting");
    }
    return 0;
}
